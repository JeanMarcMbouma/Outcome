name: Publish BbQ.Cqrs to NuGet

on:
  push:
    tags:
      - 'cqrs-v*'  # Triggers on tags like cqrs-v1.0.0, cqrs-v1.0.1
  workflow_call:    # Allow main.yml to call this workflow

permissions:
  id-token: write
  contents: read

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Restore dependencies
        run: |
          dotnet restore src/BbQ.Cqrs/BbQ.Cqrs.csproj
          dotnet restore src/BbQ.Cqrs.SourceGenerators/BbQ.Cqrs.SourceGenerators.csproj

      - name: Build
        run: |
          dotnet build src/BbQ.Cqrs/BbQ.Cqrs.csproj --configuration Release --no-restore
          dotnet build src/BbQ.Cqrs.SourceGenerators/BbQ.Cqrs.SourceGenerators.csproj --configuration Release --no-restore

      - name: Test
        run: dotnet test tests/BbQ.Cqrs.Tests/BbQ.Cqrs.Tests.csproj --configuration Release --verbosity normal

      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF_NAME#cqrs-v}" >> $GITHUB_OUTPUT

      - name: Pack BbQ.Cqrs
        run: dotnet pack src/BbQ.Cqrs/BbQ.Cqrs.csproj --configuration Release --no-build -o ./artifacts /p:PackageVersion=${{ steps.version.outputs.version }}

      - name: Pack BbQ.Cqrs.SourceGenerators
        run: dotnet pack src/BbQ.Cqrs.SourceGenerators/BbQ.Cqrs.SourceGenerators.csproj --configuration Release --no-build -o ./artifacts /p:PackageVersion=${{ steps.version.outputs.version }}

      - name: NuGet login (OIDC → temp API key)
        uses: NuGet/login@v1
        id: login
        with:
          user: ${{ secrets.NUGET_USER }}

      - name: Push to NuGet
        run: dotnet nuget push ./artifacts/*.nupkg --api-key ${{steps.login.outputs.NUGET_API_KEY}} --source https://api.nuget.org/v3/index.json --skip-duplicate

      - name: Push Symbols
        run: dotnet nuget push ./artifacts/*.snupkg --api-key ${{steps.login.outputs.NUGET_API_KEY}} --source https://api.nuget.org/v3/index.json --skip-duplicate
